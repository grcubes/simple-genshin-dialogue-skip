# English
Helps dialogue move faster
## How to use
First run this on your command line:
```
pip install -r requirements.txt
```
Then run `run.bat` as administrator

If you know what you're doing, you can also use a python virtual environment
Call it `.venv`. Create it in the same path as run.bat and everything else
It should (hopefully) work
